#include "server.h"

#include <QHostAddress>

Server::Server()
{
//    server.listen(QHostAddress::Any, 4711);
}

void Server::test()
{
//    server.call(SIGNAL(printClientInfos());
//    server.call(SIGNAL(printClientInfos(), arguemte);
}
